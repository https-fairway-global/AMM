// Placeholder: Add tsconfig.build.json content for crypto package.
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": [
    "src/**/*"
  ],
  "exclude": [
    "node_modules",
    "dist",
    "**/*.test.ts"
  ]
  // Repeat project references if any
  // "references": []
} // Example Structure 